import { CarouselSelectorDirective } from './carousel-selector.directive';

describe('CarouselSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
